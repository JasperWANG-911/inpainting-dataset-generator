{
  "steps": {
    "1": { "func": "clear_scene",
           "editable_params": {} ,
           "task_description": "Clear the current scene to start fresh.",
           "edit_hints":"If the scene is not empty, need to run clear_scene() again."
      },
    "2": { "func": "add_ground",
           "editable_params": { "size": 100 },
           "task_description": "Add a ground plane to the scene so that objects can be stick to it. A default size of 100 is used, it should be large enough to accommodate all objects.",
           "edit_hints": "If a ground is not created, need to run add_ground() again."
         }
  }
}
